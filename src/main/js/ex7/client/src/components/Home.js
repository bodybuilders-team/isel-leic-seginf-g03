/**
 * Home component.
 */
function Home() {
    return (
        <div>
            <h1>Home</h1>

            <p>
                This is a simple React app that uses the Google OAuth 2.0 API to authenticate users.
                It also uses the Google Tasks API to display the user's tasks.
            </p>
        </div>
    );
}

export default Home;